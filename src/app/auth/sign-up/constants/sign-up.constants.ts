export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export const RESTCOUNTRIES_BASE_PATH = 'https://restcountries.com/v3.1';

export const RESTCOUNTRIES_ENDPOINTS = {
  all: '/all',
  name: '/name',
};
